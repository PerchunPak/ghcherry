There is no way to write tests for the `cherry_pick_commit` method, without
just mocking every request. Those are meaningless tests and won't really catch
any bugs.

I use this tool daily anyway, so it *shouldn't* be a problem.
